<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="POSApp.ProductDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:POSApp"
    Title="Product Details"
    PrimaryButtonText="Save"
    CloseButtonText="Cancel"
    DefaultButton="Primary"
    RequestedTheme="Dark">

    <ScrollViewer MaxHeight="500" VerticalScrollBarVisibility="Hidden">
        <Grid Width="400">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Product Name -->
            <TextBlock Grid.Row="0"
                   Text="Product Name"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

            <TextBox x:Name="ProductNameTextBox"
                 Grid.Row="1"
                 PlaceholderText="Enter product name..."
                 FontSize="14"
                 VerticalAlignment="Top"
                 HorizontalAlignment="Stretch"
                 Margin="0,0,0,20"
                 CornerRadius="4">
                <TextBox.Resources>
                    <Style TargetType="TextBox">
                        <Setter Property="Background" Value="{ThemeResource SolidBackgroundFillColorBaseBrush}"/>
                        <Setter Property="BorderBrush" Value="{ThemeResource TextFillColorTertiaryBrush}"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>

            <!-- Product Category -->
            <TextBlock Grid.Row="2"
                   Text="Product Category"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8"/>

            <ComboBox x:Name="CategoryComboBox" PlaceholderText="Select a category" Grid.Row="3" Margin="0,0,0,8">
                <ComboBoxItem Content="Karahi"/>
                <ComboBoxItem Content="Handi"/>
                <ComboBoxItem Content="Tikka"/>
                <ComboBoxItem Content="Kabab"/>
                <ComboBoxItem Content="Tandoor"/>
                <ComboBoxItem Content="Drinks"/>
            </ComboBox>

            <!-- Product Price -->
            <TextBlock Grid.Row="4"
                   Text="Product Price (Rs)"
                   FontSize="16"
                   FontWeight="SemiBold"
                   />

            <NumberBox x:Name="ProductPriceNumberBox"
                   Grid.Row="5"
                   PlaceholderText="0.00"
                   FontSize="14"
                   Minimum="0"
                   Maximum="99999"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Stretch"
                   Margin="0,8,0,0">
                <NumberBox.Resources>
                    <Style TargetType="NumberBox">
                        <Setter Property="Background" Value="{ThemeResource SolidBackgroundFillColorBaseBrush}"/>
                        <Setter Property="BorderBrush" Value="{ThemeResource TextFillColorTertiaryBrush}"/>
                    </Style>
                </NumberBox.Resources>
            </NumberBox>
        </Grid>
    </ScrollViewer>
</ContentDialog>
